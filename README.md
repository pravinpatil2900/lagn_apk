# Lagn APK — Paste → APK (Super Easy)
Steps:
1) Create new GitHub repo → **Add file → Upload files** → upload this ZIP contents.
2) Press **Commit**.
3) Go to **Actions** → open latest run → **Artifacts** → download `app-release-apk`.
